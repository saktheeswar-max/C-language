#include<stdio.h>
#include<string.h>

int main()
{
   int count=0;
   char str1[10]="CAT";
   puts(str1);
   for(int i=0;str1[i]!='\0';i++)
   {
       count++;
   }
   printf("the size is %d",count);
   return 0;
}
